# 运输管理系统需求待确认文档（V2）

版本：V0.2  
用途：与业务、调度、财务、产品和研发进行需求确认  
说明：本文档严格基于本次提供的需求整理。文档中的“建议”不是已确认需求，需要评审后定稿。

---

## 1. 需求目标

客户每天产生大量运输计划，涉及多个发货地、多个收货地和多种车型。系统需要将零散计划进行合单，形成可执行的运输任务，并完成外车派车、运输跟踪、电子回单和运费结算。

目标链路为：

```text
批量导入运输计划
→ 数据清洗与地址标准化
→ 合单
→ 生成运输任务单
→ 计算客户合同价
→ 外车派车
→ 运输执行与跟踪
→ 电子回单
→ 外车结算与客户结算
```

---

## 2. 原需求中已经明确的内容

以下内容可以视为当前需求基线，但其中仍有部分实现规则需要进一步确认。

### 2.1 批量导入

- 支持 API 和 Excel 两种导入方式。
- 每天存在固定导入时段，例如上午10点、下午3点。
- 单批次约50～200条运输计划。
- 每条运输计划对应一辆设备的运输需求。

### 2.2 数据清洗

- 校验地址格式。
- 校验联系人信息完整性。
- 支持模糊地址的标准化处理，例如“长沙附近”转换为标准行政区划。

### 2.3 合单

- 业务目标是将多条计划合并成整车或多站点运输。
- 当前主要依赖调度经验合单。
- 前期优先考虑可操作的合单功能，不要求一开始实现完整自动优化算法。
- 计划合并参考同路线、相近路线、串点配送、车型兼容和装卸方式兼容。
- 合单后生成若干运输任务单。

### 2.4 运输任务单

每张任务单包含：

- 一辆车。
- 1～N条运输计划。
- 预计装车顺序。
- 预计卸货顺序。
- 总合同价，当前描述为任务内各计划合同价之和。

### 2.5 客户合同价

基础价格库以以下字段作为联合匹配条件：

```text
发车省 + 发车市 + 发车区
+ 收车省 + 收车市 + 收车区
+ 轴距划分
+ 公里划分
```

计算公式：

```text
合同价 = 基础运费 × 服务系数
```

计算结果与订单绑定，订单进入“待派车”。

### 2.6 外车派车和执行

- 车辆为外部车辆，不是自有车队。
- 订单可以发布到外部运力池或指定承运商。
- 承运商接单后进入后续执行流程。
- 当前没有司机 APP，先做司机、车辆、承运商建档和派车绑定。
- 通过电话、微信、短信推动定位和回单。

### 2.7 电子回单

- 支持签收照片和 POD 文件。
- 系统记录回单上传状态。
- 未上传回单不能发起结算。
- 司机送达并上传电子回单后，订单变为“已完成”。

### 2.8 结算

外车结算：

- 预付款 + 尾款的分阶段结算。
- 预付款可能在派车确认后或装车完成后支付。
- 示例比例为60%。
- 尾款在运输完成和电子回单确认后支付。
- 结算以运输任务单为单位，并关联实际完成的运输计划明细。
- 支持逐单结算。

客户结算：

- 合同约定价作为初始结算基准。
- 支持运输途中增补价。
- 最终结算价 = 合同约定价 + 途中增补价。

---

## 3. 当前需求中需要先解决的核心矛盾

### 3.1 MVP 是否包含合单

当前文档同时出现了两种表述：

1. 场景的核心区别是“智能合单”，系统要将多条计划合为运输任务。
2. MVP建议是“数据导入 → 派车 → 运费结算”，没有明确写入合单。

如果 MVP 不包含合单，系统无法体现本项目与单票派车系统的主要区别；但如果一期直接做全局优化算法，范围和风险又会明显扩大。

**建议确认结论：**

一期包含“人工合单 + 基础规则辅助”，不做全自动最优算法。

建议一期流程改为：

```text
数据导入
→ 数据校验
→ 待合单计划池
→ 调度选择并确认合单
→ 生成运输任务单
→ 派车
→ 回单
→ 运费结算
```

### 3.2 客户合同价与外车运费没有对称定义

需求明确了客户合同价，但没有明确外车实际承运价如何产生。外车预付款和尾款必须有一个“外车应付金额”作为计算依据，不能直接默认等于客户合同价。

**建议确认结论：**

明确区分两种金额：

```text
客户合同价：企业向客户收取的金额，按运输计划维度
外车承运价：企业支付给承运商的金额，按运输任务单维度
```

两者可以使用不同的价格规则，也可以存在毛利差额。

### 3.3 “承运商接单后进入运输中”可能不符合实际状态

承运商接单只代表接受运输任务，通常还没有装车和发车。若直接进入“运输中”，会影响定位、超时、回单和结算判断。

**建议确认结论：**

至少区分：

```text
待派车
→ 已发布 / 待接单
→ 承运商已接单
→ 待装车
→ 已装车
→ 运输中
```

“运输中”建议以发车确认作为触发条件。

### 3.4 “强制定位”与无司机 APP 存在实现冲突

当前需求一方面说明没有司机 APP，只能通过电话、微信、短信推动定位；另一方面开发沟通中又提出“强制定位回传”。

普通电话、微信聊天和短信本身不能保证持续定位。要实现强制或自动定位，至少需要以下一种能力：

- 司机点击微信/H5链接并授权定位。
- 司机小程序或 APP。
- 第三方车辆 GPS 平台。
- 承运商提供定位接口。

**建议确认结论：**

一期将“强制定位”改为“关键节点定位 + 人工跟进”；实时定位、偏离路线和自动无定位预警放到有定位数据源后实现。

---

## 4. 业务对象和数据关系待确认

原需求中同时使用了“运输计划”“订单”“运输任务单”，必须先统一名称和关系。

### 4.1 建议对象定义

| 对象 | 建议含义 | 需要确认 |
|---|---|---|
| 运输计划 | 客户提供的一条设备运输需求 | 一条计划是否可能包含多台设备 |
| 运输任务单 | 合单后交给一辆车执行的任务 | 是否允许跨客户或跨合同合单 |
| 订单 | 业务上是否仍需要独立对象 | 是否只是运输计划的旧称 |
| 派车记录 | 任务与承运商、司机、车辆的绑定记录 | 换车后是修改还是新增记录 |
| 回单记录 | 计划或卸货点的签收资料 | 按计划还是任务管理 |
| 客户结算单 | 向客户结算的金额和增补费用 | 按计划、任务还是账期结算 |
| 外车结算单 | 支付承运商的金额 | 是否需要对接财务系统 |

### 4.2 建议默认关系

```text
一个运输任务单
= 一辆车 + 一个承运商 + 一个司机 + 1~N条运输计划
```

建议一期一条运输计划只能完整地归属于一个运输任务。若有拆分需求，先拆成多条计划再合单。

---

## 5. 批量导入待确认项

| 编号 | 待确认问题 | 建议方案 | 影响 |
|---|---|---|---|
| I-01 | 固定时间是自动导入还是业务手动触发 | 支持定时批次，也支持临时手动补单 | 导入、任务调度 |
| I-02 | 一批中部分数据错误时是否整批失败 | 正确数据入库，错误数据单独返回 | 导入、异常 |
| I-03 | 如何防止同一计划重复导入 | 使用客户编号 + 外部计划编号做幂等校验 | 接口、数据 |
| I-04 | Excel模板由谁维护 | 系统提供模板、示例和字段说明 | 导入、培训 |
| I-05 | 错误数据如何修正 | 下载错误明细，修改后重新导入 | 导入、业务操作 |
| I-06 | 导入后哪些字段可以修改 | 地址、车型、数量等关键字段修改后重新校验和计价 | 计划、计价 |
| I-07 | 已派车计划是否允许再次覆盖 | 不允许直接覆盖，走变更流程 | 派车、审计 |
| I-08 | API是否需要回传处理结果 | 返回批次号、成功数、失败数和逐行错误 | 接口 |

### 建议的批次结果

导入完成后至少展示：

- 总记录数。
- 成功记录数。
- 重复记录数。
- 字段错误数。
- 地址待确认数。
- 价格无法匹配数。
- 可进入待合单池的记录数。

---

## 6. 地址标准化和里程待确认项

### 需要确认

1. 使用哪家地图或地址标准化服务。
2. 地址标准化到省、市、区县，还是需要街道和门牌号。
3. “长沙附近”等模糊地址是否由系统自动决定，还是由业务员确认候选地址。
4. 无法标准化的地址是否允许继续合单。
5. 里程采用地图推荐里程、最短里程还是实际行驶里程。
6. 多站点运输的公里数如何计算。
7. 地址确认后是否需要重新计价。

### 建议方案

保留以下两份地址：

```text
原始地址：客户提交的原文，不能覆盖
标准地址：确认后的行政区划、详细地址和坐标
```

模糊地址建议采用“系统给候选 + 业务员确认”，不要直接静默转换。未确认地址不建议进入正式计价和派车。

---

## 7. 合单待确认项

### 7.1 合单范围

需要确认：

- 只能在同一批次内合单，还是当天所有未派车计划都可以合单。
- 上午10点批次和下午3点批次能否合并。
- 是否允许跨天合单。
- 已加入任务但尚未派车的计划能否重新合单。
- 派车确认后是否禁止拆单。
- 业务员能否将计划标记为“不可合单”。

**建议：**使用统一待合单计划池；未锁定、未派车计划可以调整；已装车计划不能重新合单。

### 7.2 合单规则

当前只描述了方向，需要继续明确可执行阈值：

- “相近发货地”按多少公里定义。
- “相近收货地”按多少公里定义。
- 最大允许绕行多少公里或多少比例。
- 一辆车最多几个装货点和卸货点。
- 是否允许跨省串点。
- 是否允许跨客户和跨合同。
- 是否有装货和送达时间窗口。
- 是否考虑总重量、体积、设备尺寸和车辆载重。
- 哪些装卸方式不能兼容。
- 哪些设备不能混装。
- 是否考虑司机驾驶时间和休息要求。

**建议：**一期将系统定位为“合单辅助工具”，支持：

1. 业务员选择计划。
2. 系统展示路线、车型、时间和装载提示。
3. 系统给出推荐或风险提示。
4. 调度人工确认。
5. 系统生成运输任务草稿。

强制合单需要填写原因并保留操作日志。

### 7.3 装车和卸货顺序

需要确认：

- 顺序由系统自动推荐还是完全由调度安排。
- 是否遵循“后装先卸”。
- 收货方是否有优先级。
- 调整顺序后是否重新计算路线和价格。
- 运输过程中是否允许临时调整顺序。

**建议：**系统给出默认顺序，调度可以调整；若违反后装先卸或增加明显绕行，系统提示风险并记录原因。

---

## 8. 车型、轴距、司机和车辆字段待确认

当前需求将司机准驾、车辆车型、轴距分组放在同一段中，技术上需要拆成不同字段。

### 8.1 司机字段

- 司机姓名。
- 身份证号。
- 手机号。
- 驾驶证准驾车型，例如 A2、C1。
- 驾驶证有效期。
- “是否人驾”的准确含义。

### 8.2 车辆字段

- 车牌号。
- 车辆长度。
- 车厢类型，例如高栏、平板等。
- 轴数。
- 轴距。
- 最大载重。
- 是否具备叉车、吊装等装卸能力。
- 车辆证件有效期。

### 8.3 必须确认

1. A2、C1是司机准驾类型，还是内部车型分类。
2. “轴距划分”具体有哪些分组。
3. 价格匹配依据是车长、轴距、轴数、载重还是内部车型组。
4. 是否需要维护司机和车辆证件有效期并到期拦截。
5. 是否存在特殊设备或不能混装的设备。

---

## 9. 客户合同价待确认项

### 当前明确规则

```text
基础运费 = 根据起点、终点、轴距划分、公里划分查询价格库
合同价 = 基础运费 × 服务系数
```

### 需要确认

1. 客户合同价是否严格按运输计划维度计算。
2. 一个客户是否存在多个合同或多个价格版本。
3. 服务系数由客户、线路、车型、时效还是服务类型决定。
4. 价格库是否需要生效日期和失效日期。
5. 找不到价格时订单进入什么状态。
6. 是否允许人工补价。
7. 人工补价是否需要审批。
8. 合单后是否存在整车优惠。
9. 多点卸货是否产生客户侧加站费用。
10. 任务总合同价是否始终等于计划合同价之和。

### 建议方案

一期按计划计算并保存计算快照：

```text
计划合同价 = 基础运费 × 服务系数
任务合同价 = 任务内各计划合同价之和
```

每次计算保存使用的价格版本、里程、服务系数和基础运费，避免价格库后续修改影响历史订单。

---

## 10. 外车承运价待确认项

这是当前需求中缺失但必须补充的一组规则。

### 需要确认

1. 外车承运价由谁提供：系统价格库、调度询价还是承运商报价。
2. 外车承运价按运输计划还是运输任务单计算。
3. 是否允许承运商报价和调度议价。
4. 最终承运价由谁确认、谁审批。
5. 承运价是否包含油费、路桥费、装卸费和税费。
6. 预付款和尾款基于哪个金额计算。
7. 承运商途中增补费用是否单独管理。
8. 取消任务时预付款如何处理。

### 建议方案

按运输任务单维护外车应付：

```text
外车最终应付
= 任务承运价
+ 承运商审批通过的增补费用
```

预付款和尾款只是在这笔外车应付上的分阶段支付，不应使用客户合同价直接代替外车承运价。

---

## 11. 派车和执行状态待确认项

### 需要确认

- 外部运力池是否已经存在。
- 承运商是否需要登录系统。
- 承运商是自主接单还是由调度电话确认后代录。
- 是否允许同时发布给多个承运商。
- 拒单、超时未接单如何处理。
- 临时换司机、换车辆如何处理。
- 司机和车辆是否必须先建档才能派车。

### 建议一期状态

```text
待派车
→ 已发布 / 待接单
→ 承运商已接单
→ 司机车辆已绑定
→ 待装车
→ 已装车
→ 已发车 / 运输中
→ 待回单
→ 已完成
```

建议一期由调度指定承运商并绑定司机车辆，不要求承运商端自主接单；外部运力池和抢单功能后置。

---

## 12. 定位和异常预警待确认项

### 12.1 定位

需要确认：

1. “强制定位回传”是产品硬性要求还是后续目标。
2. 定位通过微信/H5、司机小程序、司机 APP、第三方 GPS 还是承运商接口实现。
3. 定位是发车和到达等关键节点，还是持续实时定位。
4. 司机拒绝定位授权是否允许继续执行。
5. 多久没有位置更新算无定位。
6. 由谁处理无定位任务。

### 建议一期方案

在没有司机 APP 或 GPS 数据源的情况下，将范围定义为：

- 司机/调度确认装车。
- 发车时记录一次位置或人工确认。
- 到达装货点、卸货点时记录节点。
- 通过微信/H5链接推动司机主动上报位置。
- 业务员记录电话、微信、短信跟进结果。

### 12.2 异常预警

当前“偏离路线、无定位、超时”均标为待定。需要确认：

- 哪个阶段建设。
- 预警数据来源。
- 路线基准如何生成。
- 偏离多少公里或多少分钟算异常。
- 超时以哪个时间点为准。
- 预警通知给业务员、调度还是主管。
- 异常是否需要关闭原因和处理记录。

**建议：**一期先做人工跟进提醒和节点超时提醒；接入稳定定位后再做偏离路线和自动无定位预警。

---

## 13. 回单管理待确认项

### 需要确认

1. 回单按订单、运输计划还是卸货点管理。
2. 一张任务单包含多条计划时，是否每条计划都必须有回单。
3. 支持哪些文件：签收照片、POD、盖章文件、电子签名等。
4. 回单由司机、承运商、业务员还是客服上传。
5. 谁审核回单。
6. 审核不通过是否允许退回补传。
7. 部分计划有回单时能否部分结算。
8. 回单是否必须客户确认。
9. 回单资料保存多久。

### 建议方案

由于一个运输任务可能包含多个运输计划和多个收货点，建议回单最少按“运输计划 / 卸货点”记录：

```text
计划1：已送达，回单已审核
计划2：已送达，回单待审核
计划3：未送达
任务：部分完成
```

MVP建议整张任务单的所有计划回单审核通过后，才允许发起尾款结算；部分结算可以后续增加。

---

## 14. 结算待确认项

### 14.1 外车结算

需要确认：

- 预付款触发点是派车确认还是装车完成。
- 预付款比例是否固定60%，还是按承运商/合同配置。
- 尾款是否必须等待所有计划回单审核通过。
- 是否需要财务审批。
- 是否支持逐任务结算、批量结算。
- 是否需要发票、对账单、付款凭证。
- 外车增补费用是否在尾款中结算。

建议默认流程：

```text
装车完成
→ 申请预付款
→ 支付预付款
→ 全部计划送达
→ 回单审核通过
→ 增补费用审批完成
→ 申请尾款
→ 支付尾款
```

### 14.2 客户结算

需要确认：

- 客户按计划、任务还是账期结算。
- 客户增补价是否需要客户确认。
- 客户增补价是否需要审批。
- 是否需要开票和对账。
- 是否允许按计划单独结算。

建议默认：

```text
客户最终结算价
= 计划合同价之和
+ 审批通过的客户途中增补价
```

客户增补价与承运商增补价应分开保存，二者金额和审批人可以不同。

---

## 15. 异常流程待确认项

以下异常在原需求中没有定义处理方式，需要业务补充：

- 导入重复。
- 地址无法识别。
- 联系人缺失。
- 价格库无匹配。
- 车型不兼容。
- 无法合单。
- 承运商拒单。
- 长时间未接单。
- 派车后换车或换司机。
- 装车失败。
- 临时改址、取消或加站。
- 车辆故障。
- 司机无定位。
- 运输超时。
- 部分站点未送达。
- 回单缺失。
- 回单审核不通过。
- 已支付预付款后任务取消。
- 客户增补费用未审批。

每个异常需要明确：

1. 谁可以发起。
2. 谁负责处理。
3. 是否需要审批。
4. 是否允许继续运输。
5. 是否影响客户合同价。
6. 是否影响外车承运价。
7. 是否需要上传证明。
8. 是否需要保留原始记录。

---

## 16. 状态建议

原需求中直接使用了“待派车、运输中、已完成”，但为了支持合单、回单和结算，建议将状态拆开。

### 16.1 运输计划状态

```text
导入待校验
→ 数据异常 / 地址待确认
→ 待合单
→ 已加入运输任务
→ 执行中
→ 已送达
→ 回单审核中
→ 已完成
→ 已取消
```

### 16.2 运输任务状态

```text
合单草稿
→ 待计价
→ 待派车
→ 待承运商确认
→ 待装车
→ 已装车
→ 运输中
→ 待回单
→ 已完成
→ 结算中
→ 已结算
```

### 16.3 结算状态

```text
未结算
→ 预付款待申请
→ 预付款已支付
→ 尾款待申请
→ 尾款已支付
→ 已结清
```

需要业务重点确认：

- 承运商接单是否进入运输中：建议不进入。
- 司机发车是否进入运输中：建议进入。
- 送达但无回单是否已完成：建议未完成。
- 回单上传但未审核是否可以发起尾款：建议不可以。

---

## 17. MVP建议（严格按当前需求收敛）

### 17.1 一期建议建设

```text
Excel/API批量导入
→ 数据校验和地址确认
→ 人工合单 / 基础规则辅助
→ 生成运输任务单
→ 客户合同价计算
→ 指定承运商和司机车辆绑定
→ 装车、发车等关键节点记录
→ 电子回单上传和审核
→ 外车按任务单逐单结算
→ 客户按计划计算合同价和增补价
```

### 17.2 一期建议不承诺

- 全自动智能合单。
- 自动最优路线。
- 承运商抢单、竞价和自主接单平台。
- 司机 APP。
- 持续实时定位。
- 自动偏离路线预警。
- 自动无定位预警。
- 复杂的部分计划结算。
- 自动付款和完整发票流程，除非已有明确财务接口。

### 17.3 为什么建议一期保留人工合单

合单是本系统区别于单票模式的核心业务。如果一期完全跳过合单，后续再接入合单时，运输任务、价格、派车和回单的数据关系可能需要重做。

一期不需要做复杂算法，但需要先把以下能力建好：

- 待合单计划池。
- 计划选择和移除。
- 合单草稿。
- 车辆和装卸兼容提示。
- 装车、卸货顺序调整。
- 任务单生成。

---

## 18. 本次评审建议必须形成的结论

建议至少确认以下内容：

1. MVP 是否包含人工合单。
2. 运输计划、订单、运输任务单的定义和关系。
3. 一条计划是否允许拆分。
4. 一个任务是否只对应一辆车。
5. 是否允许跨客户、跨合同合单。
6. 合单是在同一批次还是全日计划池中完成。
7. 相近路线、最大绕行和最大站点数标准。
8. 车型、轴距、轴距划分和 A2/C1 的准确含义。
9. 客户合同价的匹配和版本规则。
10. 外车承运价如何产生和审批。
11. 承运商接单的具体方式。
12. “运输中”的准确触发节点。
13. 当前定位采用什么技术方案。
14. 异常预警是否属于一期。
15. 回单按计划、卸货点还是任务管理。
16. 部分回单是否允许部分结算。
17. 预付款的准确触发点和比例。
18. 客户增补价和承运商增补价是否分开管理。
19. 是否需要财务系统、发票和付款接口。
20. 角色权限和金额审批边界。

---

## 19. 推荐默认基线（无法立即决定时使用）

| 事项 | 建议默认方案 |
|---|---|
| MVP | 导入、地址确认、人工辅助合单、计价、派车、关键节点、回单、结算 |
| 计划拆分 | 一期不允许，拆分先转成多条计划 |
| 任务车辆 | 一张任务单一辆车 |
| 合单范围 | 当天未锁定、未派车计划池 |
| 跨客户合单 | 一期不允许 |
| 跨合同合单 | 一期不允许 |
| 合单方式 | 系统提示，调度人工确认 |
| 客户合同价 | 按运输计划计算 |
| 外车承运价 | 按运输任务单计算 |
| 派车方式 | 调度指定承运商并绑定司机车辆 |
| 运输中触发 | 发车确认后 |
| 定位方式 | 关键节点定位 + 电话/微信/短信跟进 |
| 回单方式 | 按计划或卸货点上传和审核 |
| 部分结算 | 一期不支持 |
| 预付款 | 装车完成后申请，默认60%，可配置 |
| 尾款 | 全部计划回单审核通过后申请 |
| 客户增补价 | 独立审批后计入客户应收 |
| 承运商增补价 | 独立审批后计入外车应付 |
| 自动异常预警 | 后置，先做人工提醒和操作记录 |

---

## 20. 评审记录

| 日期 | 参与人 / 部门 | 确认事项 | 未决事项 | 负责人 | 截止时间 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |
|  |  |  |  |  |  |
|  |  |  |  |  |  |

### 关键确认签字

业务代表：________________  日期：______________

调度代表：________________  日期：______________

财务代表：________________  日期：______________

产品代表：________________  日期：______________

技术代表：________________  日期：______________

