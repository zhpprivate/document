# 工程协作约定

## 图表文字方向

- 所有流程图、架构图、组件图及其他可视化图表，文字必须横向排列。
- 禁止使用竖排文字、旋转90度或负90度的文字，包括泳道标题、分区标题、节点标题、连线标签和图例。
- draw.io图中不得使用 `horizontal=0`、`rotation=90`、`rotation=-90` 等会产生竖排或旋转文字的样式。
- SVG中不得使用 `rotate(90 ...)`、`rotate(-90 ...)`、纵向 `writing-mode` 或其他等效的竖排文字设置。
- 分区和泳道名称必须放在顶部横向标题栏中，不能放在左侧竖向标题栏中。
- 文字较多时，应优先加宽节点、按完整词组换行或将详细说明移到配套文档；不得通过窄框把文字挤成单字竖列。
- 交付 `.drawio.svg` 文件前，必须同时检查可见SVG和内嵌draw.io源数据，确认不存在竖排或旋转文字。
